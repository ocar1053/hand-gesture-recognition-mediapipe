mediapipe==0.10.14
opencv-python>=3.4.2
tensorflow>=2.3.0

# Optional: only when creating a TFLite for an LSTM model
# tf-nightly>=2.5.0.dev

# Optional: only if you want to display the confusion matrix
scikit-learn>=0.23.2
matplotlib>=3.3.2

# Optional: only if you want to publish MediaPipe output to rosbridge
roslibpy>=1.8.1
